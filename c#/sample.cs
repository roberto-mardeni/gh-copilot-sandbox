using System;

namespace sample
{
    public static class Program
    {
        public static long calculateDaysBetweenDates(DateTime begin, DateTime end)

        public static long calculateMinutesBetweenDates(DateTime begin, DateTime end)
    
    }
}